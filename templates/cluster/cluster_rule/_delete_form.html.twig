<form method="post" action="{{ path('admin_cluster_rule_delete', {'cluster': cluster_rule.cluster}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cluster_rule.cluster) }}">
    <button class="btn">Delete</button>
</form>
