{% extends 'base.html.twig' %}

{% block title %}Segment{% endblock %}

{% block body %}
    <h1>Segment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>VoModeleId</th>
                <td>{{ segment.voModeleId }}</td>
            </tr>
            <tr>
                <th>SegmentName</th>
                <td>{{ segment.segmentName }}</td>
            </tr>
            <tr>
                <th>SegmentClass</th>
                <td>{{ segment.segmentClass }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_cluster_segment_index') }}">back to list</a>

    <a href="{{ path('admin_cluster_segment_edit', {'voModeleId': segment.voModeleId}) }}">edit</a>

    {{ include('cluster/segment/_delete_form.html.twig') }}
{% endblock %}
