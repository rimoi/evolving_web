<form method="post" action="{{ path('admin_cluster_segment_delete', {'voModeleId': segment.voModeleId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ segment.voModeleId) }}">
    <button class="btn">Delete</button>
</form>
