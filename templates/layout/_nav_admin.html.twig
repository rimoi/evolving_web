<nav class="main-header navbar navbar-expand navbar-white navbar-light">
    <!-- Left navbar links -->
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
        </li>
    </ul>
</nav>
<!-- /.navbar -->

<!-- Main Sidebar Container -->
<aside class="main-sidebar sidebar-dark-primary elevation-4">
    <!-- Brand Logo -->
    <a href="{{ path('admin_cluster_rule_index') }}" class="brand-link">
        <span class="brand-text">Sidi - TEST</span>
    </a>

    {% set route_name = app.request.attributes.get('_route') %}

    <!-- Sidebar -->
    <div class="sidebar">
        <!-- Sidebar Menu -->
        <nav class="mt-2">
            <ul class="nav nav-pills nav-sidebar flex-column" data-widget="treeview" role="menu" data-accordion="false">
                <!-- Add icons to the links using the .nav-icon class
                     with font-awesome or any other icon font library -->

                <li class="nav-header">Règles</li>
                <li class="nav-item">
                    <a href="{{ path('admin_cluster_rule_index') }}" class="nav-link  {{ route_name starts with 'admin_cluster_rule_' ? 'active' }}">
                        <i class="nav-icon fas fa-tachometer-alt"></i>
                        <p>
                            Règle de clusterisation
                        </p>
                    </a>
                </li>

                <li class="nav-header">Clusterisations</li>
                <li class="nav-item">
                    <a href="{{ path('admin_cluster_fuel_index') }}" class="nav-link {{ route_name starts with 'admin_cluster_fuel_' ? 'active' }}">
                        <i class="nav-icon fas fa-tachometer-alt"></i>
                        <p>
                            Carburant
                        </p>
                    </a>
                </li>

                <li class="nav-item">
                    <a href="{{ path('admin_cluster_segment_index') }}" class="nav-link {{ route_name starts with 'admin_cluster_segment_' ? 'active' }}">
                        <i class="nav-icon fas fa-th"></i>
                        <p>
                            Segment
                        </p>
                    </a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('app_logout') }}" class="nav-link">
                        <i class="nav-icon fas fa-ellipsis-h"></i>
                        <p>
                            Déconnexion
                        </p>
                    </a>
                </li>
            </ul>
        </nav>
        <!-- /.sidebar-menu -->
    </div>
    <!-- /.sidebar -->
</aside>